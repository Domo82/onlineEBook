<%= form_with(model: book, local: true, html: {class: 'form-signin'}) do |form| %>
  <% if book.errors.any? %>


      <div id="error_explanation">
        <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

        <ul>
          <% book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

  <div class="text-center">

      <%= form.text_field :name, require: true, label: false, placeholder: 'Name' %>

      <%= form.text_field :author, placeholder: 'Author' %>

      <%= form.text_area :description, placeholder: 'Description' %>

      <%= form.number_field :price, placeholder: 'Price' %>

    <br>

      <%= form.label :availability, require: true %>
      <%= form.check_box :availability %>

    <br>

      <%= form.submit class: 'btn btn-primary'%>

  </div>

<% end %>
